# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo K8ssssss !
  displayName: 'Run a one-line script'
- task: DownloadSecureFile@1
  name: publickey
  inputs:
    secureFile: 'key.pub'
- task: TerraformCLI@0
  inputs:
    command: 'init'
    workingDirectory: '$(System.DefaultWorkingDirectory)/configuration/iaac/azure/kubernetes'
    commandOptions: '-var client_id=$(client_id) -var client_secret=$(client_secret) -var ssh_public_key=$(publickey.getSecureFilePath)'
    backendType: 'azurerm'
    backendServiceArm: 'pobakyd-sc-service-connection'
    backendAzureRmSubscriptionId: 'c1cb15c0-ba91-43de-9d1d-6339e77df8e8'
    ensureBackend: true
    backendAzureRmResourceGroupName: 'backend-resource-group'
    backendAzureRmResourceGroupLocation: 'westeurope'
    backendAzureRmStorageAccountName: 'backend-storage-pobakyd'
    backendAzureRmContainerName: 'backend-storage-pobakyd-container'
    backendAzureRmKey: 'kubernetes.tfstate'
    allowTelemetryCollection: true

